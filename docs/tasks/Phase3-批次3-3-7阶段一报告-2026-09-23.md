# 批次 3 · 3-7 阶段一报告：tabs.ts 拆出关窗守卫与双 rAF 工具（纯搬移）

日期：2026-09-23 ｜ 执行：modu-shell ｜ Lead 放行：阶段一（纯搬移）

## 结论
`tabs.ts` **621 → 528 行**；新增 `src/app/close-guard.ts`（92 行）与 `src/app/paint-timing.ts`（18 行）。
**导出名一个未改** ⇒ 引用它们的锚断言**一行未动**（只改 import 路径）✓ 棘轮 **hard 700 → 590** ✓
三项证明：**逐字搬移 ✓ / 门禁逐项对照 ✓ / 真机 e2e 未覆盖（如实声明）⚠**

## 一、搬了什么
| 新文件 | 内容 | 行数 |
|---|---|---|
| `src/app/close-guard.ts` | `CloseChoice` / 处置结论类型 + `shouldGuardClose` / `resolveCloseAction`（纯函数）+ `CloseGuardDeps` + `createCloseGuard`（状态机）| 92 |
| `src/app/paint-timing.ts` | `whenPainted`（双 rAF：本帧落 DOM+绘制，下一帧同步重渲）| 18 |
`tabs.ts`：删上述两段、改为 `import { whenPainted } from "./paint-timing";` ⇒ **528 行** ✓

## 二、零行为变化的证明（Lead 要求的三样）
1. **逐字搬移证明** ✓
   - `close-guard` 段：**逐字相同 = true**（**2747 == 2747**）
   - `whenPainted` 段：**340 − 333 = 7 = `export `**（即逐字一致 + 必需的导出前缀）
2. **门禁逐项对照** ✓（改前基线先落盘，再逐项比）
   | 项 | 改前基线 | 改后 |
   |---|---|---|
   | Test Files | 38 passed | **38 passed** ✓ |
   | Tests | 530 passed \| 1 skipped | **530 passed \| 1 skipped** ✓ |
   | EXIT | 0 | **0** ✓ |
3. ⚠ **真机 e2e（关窗守卫三选一）**：**未覆盖** ✗ —— 本项目**没有**关窗守卫的自动化真机探针
   （既有覆盖是**单测两层**：`tests/tabs.spec.ts`（纯函数 + 状态机）+ `tests/close-confirm.spec.ts`（浮层））
   ⇒ **不作"e2e 通过"的声明** ✓；如需真机守卫回归，需**新写**一个探针（先确认标签确实变脏，否则会把窗口真关掉 ✗），
   并在**改前**落基线 ⇒ 建议放进阶段二那一轮一起做 ✓

## 三、⚠ 过程中门禁抓到的三处（如实，属"搬移清单不全"族）
| # | 问题 | 根因 | 处置 |
|---|---|---|---|
| 1 | `paint-timing.ts` **没有 export** ⇒ `TS2306: not a module` | 我的逐字证明**已经指出**"差异恰是 `export `"，**我却没真去改** ✗ | 补 `export ` ✓ |
| 2 | `ui/close-confirm.ts` 仍从 `../app/tabs` 导 `CloseChoice` ⇒ `TS2305` | 我搬移前只 grep 了 4 个符号，**漏了随段搬走的类型** ✗ | 改指 `close-guard` ✓ |
| 3 | `tests/tabs.spec.ts` 同类（`type CloseChoice`）⇒ `TS2305` | 同上 | 改指 `close-guard` ✓ |
⇒ 两条教训：**① 证明指出的差异必须当场处置**（不能只"证明一致"就完事）✗
          **② 搬移前的引用清单要包含"随段搬走的类型/常量"，并按名字全仓 grep**（与"删机制要全仓 grep"同族）✓

## 四、棘轮
`tests/css-budget.spec.ts`：`src/app/tabs.ts` **hard 700 → 590**（只往紧的方向转 ✓；现 528）

## 五、覆盖边界（如实声明）
- **已覆盖**：TS 类型检查、单测（含关窗守卫纯函数 + 状态机 + 浮层）、lint
- **未覆盖**：真机"点 ✕ / Alt+F4 → 三选一浮层 → 保存/放弃/取消" 的端到端路径 ✗（无自动化探针）
- **推断（非实测）**：搬移为逐字 + 导入路径调整 ⇒ 运行时行为应完全一致（**由逐字证明支撑，但未经真机复验**）✓

## 六、阶段二（待 Lead 过 deps 清单后再动）
`tab-dnd.ts`(72) / `tab-nav.ts`(61) / `tab-dom.ts`(58) ⇒ `tabs.ts` ≈ **352**（进 soft 400），hard → 420。
⚠ 阶段二**必然改签名**（闭包捕获 → 显式 deps）⇒ 需逐项 deps 清单 + 双向 1:1 核对 + 穷尽替换表；
⚠ DnD 的**真机拖放无法用 CDP 注入**（既有环境边界）⇒ 只能靠 body 逐字 + deps 1:1 + 单测三层，
   报告须声明"真机拖放未被 e2e 覆盖" ✗
