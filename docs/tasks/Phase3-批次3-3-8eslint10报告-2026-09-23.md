# 批次 3 · 3-8 报告：eslint 9.39.5 → 10.11.0

日期：2026-09-23 ｜ 执行：modu-shell ｜ 类型：devDependency 升级（**无产品代码改动**）

## 结论（一句话）

**升级成功且"干净"**：`eslint ^9.39.5` → **`^10.11.0`**（实际安装 10.11.0），
`pnpm exec eslint .` ⇒ **0 error / EXIT 0**，warning 仍是**既有那一族**（见下，**数量不写死**）；
**没有触发任何新规则**、**没有做任何真实修改**、**没有放宽规则、没有加 ignore**（`eslint.config.js` **一字未改**）。

## 一、先查阻塞（先查后动）

| 项 | 实测 |
|---|---|
| 目标版本 | registry `latest` = **10.11.0**（`npm view eslint version`） |
| 插件是否支持 | 已装 **且** registry 最新的 `typescript-eslint@8.70.1`，其 `peerDependencies` = `eslint: "^8.57.0 \|\| ^9.0.0 \|\| ^10.0.0"` ⇒ **本来就含 `^10.0.0`** ✓ ⇒ **无需升插件** ✓ |
| Node engines | eslint 10 要求 `^20.19.0 \|\| ^22.13.0 \|\| >=24`；本机 `node v24.14.0` ✓ |
| 配置形态 | 本项目用 **flat config**（`eslint.config.js`）⇒ v10 **移除 eslintrc 支持**对我们**无影响** ✓ |

⇒ 三条前置全过，才动手（避免"升完发现插件不兼容"的返工）。

## 二、升级结果

| 项 | 结果 |
|---|---|
| `pnpm add -D eslint@10.11.0` | **EXIT 0**；`package.json` → `^10.11.0`；`require('eslint/package.json').version` = **10.11.0** ✓ |
| `pnpm exec eslint .` | **0 errors**，EXIT 0 ⇒ **各规则均未被触发为 error** ✓ |
| **新规则触发了哪些** | **无** —— 与 9.39.5 的基线**同类同量级**（warning 全是既有 `max-lines-per-function` / `max-lines` 族，例：`tests/view.spec.ts:137` 的 81 > 50）✓ |
| **哪些是真改** | **无**（0 error ⇒ 无需修改源码）✓ |
| **哪些是抑制（+理由）** | **无抑制、无新增 ignore** ✓ —— 证据：`git status` 只有 `package.json` + `pnpm-lock.yaml`，**`eslint.config.js` 未出现在改动里** ✓ |
| 既有 warning 是否清 | **不清**（按 Lead 口径：属既有 `max-lines` 族）✓ —— 数量**不写进文档**，要看就 `pnpm exec eslint .` 现跑 ✓ |

## 三、传递依赖变化（并入 backlog 的"新 devDeps 评审记录"）

**新增（全部 dev-only，无运行时依赖）**：
`eslint@10.11.0`、`espree@11.2.0`、`eslint-scope@9.1.2`、`file-entry-cache@11.1.5`、`flat-cache@6.1.23`、
**`cacheable@2.5.0` + `@cacheable/memory@2.2.0` + `@cacheable/utils@2.5.0`**、
**`keyv@5.6.0` + `@keyv/bigmap@1.3.1` + `@keyv/serialize@1.1.1`**、`hashery@1.5.1`、`hookified@{1.15.1,2.2.0}`、
`@types/esrecurse@4.3.1`，以及 `@eslint/*` 家族的版本抬升（`config-array@0.23.5`、`config-helpers@0.7.0`、`core@1.2.1`…）

**移除**：`@eslint/eslintrc@3.3.7`（**v10 移除 eslintrc 支持**）、`@eslint/js@9.39.5`、`ansi-styles@4.3.0`、
`balanced-match@1.0.2`、`brace-expansion@1.1.21`

⇒ **体积/红线影响：无** —— eslint 是 **devDependency**，不进 MSI ✓（且本轮**未改任何产品文件** ⇒ MSI 不可能受影响 ✓）
⇒ 新增的重点是 `file-entry-cache v11` 带来的 **cacheable/keyv** 缓存栈（dev-only）—— 记一笔备查 ✓

## 四、验收（三项门禁，EXIT 码；按新规矩不写计数）

| 门禁 | 命令 | 结果 |
|---|---|---|
| TS/单元/lint | `cd modu && pnpm run check`（含 `eslint .`） | **EXIT 0** |
| Rust | `cd modu/src-tauri && cargo test`（跑前已清残留 `msedgewebview2`） | **EXIT 0** |
| 敏感信息 | `cmd /c scripts\check-sensitive.cmd` | **EXIT 0** |

改动面：`modu/package.json`、`modu/pnpm-lock.yaml`（**仅此两个**）✓

## 五、遗留

- **warning 未清**（既有 `max-lines` 族）：不在 3-8 范围 ✓；3-7 拆文件时会**顺带减少一部分**（拆完记得复核 warning 是否下降，但**仍不写死数量**）✓
- 本报告**不写测试数**（按本轮新立的规矩：门禁数值一律现跑）✓
