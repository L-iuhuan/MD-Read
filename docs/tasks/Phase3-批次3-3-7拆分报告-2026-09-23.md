# 批次 3 · 3-7 报告：拆 `main.ts`（第一段：顶栏拥挤态外移）

日期：2026-09-23 ｜ 执行：modu-shell ｜ 类型：**纯重构（零行为变化，有逐字证据）**

## 结论（一句话）

把 `main.ts` 的「**顶栏拥挤态**」整段（71 行）搬到新模块 `src/app/shell-overflow.ts`，
并把共用的取元素助手抽成 `src/app/dom.ts` ⇒ **`main.ts` 811 → 738 行**；
**搬移内容与改前逐字相同**（唯一差异是新模块必需的 `export ` 前缀 7 字符）⇒ **零行为变化** ✓
**棘轮已收紧**（`main.ts` hard **900 → 780**，只往紧的方向转）✓ 三项门禁全绿 ✓

## 一、为什么选这一段（不是"半拆"）

| 判据 | 实测 |
|---|---|
| 依赖面 | 该段**只用**到 `$`（取元素助手）+ DOM id ⇒ 唯一跨依赖是这个**平凡助手** ⇒ 可整段、完整外移 ✓ |
| 是否自洽 | 常量（`CHROME_RESERVE_DEFAULT` / `TABS_FIT_DEADBAND`）+ 两个纯函数（`chromeReserveWidth` / `desiredTabsWidth`）+ `setupShellOverflow` 全在该段内 ⇒ 无残留引用 ✓ |
| 使用面（`git grep -- '*.ts'`） | 这 4 个符号**只被本段用**；外部唯一引用是注释（`ui/tabs-menu.ts:122`）+ 静态锚（`tests/panel-unify.spec.ts`）✓ |
| 是否动行为 | **不动**：逐字搬移（见 §三）✓ |

⚠ 纪律遵守：**没有半拆**（Lead 的安全阀）—— 要么整段完整外移、锚同步迁移，要么原样不动 ✓

## 二、改动清单

| 文件 | 变化 |
|---|---|
| `src/app/dom.ts`（新，18 行） | `req<T>(id)` —— 与 `main.ts` 原 `$` **语义逐字一致**（缺失时 `console.error` 记技术细节 + 抛面向使用者的中文错） |
| `src/app/shell-overflow.ts`（新，79 行） | 顶栏拥挤态整段（含**全部原始注释与实测读数**，未删未改）+ `req` 导入；`setupShellOverflow` 导出 |
| `src/main.ts` | `$` 改为 `const $ = req;`（**原有调用点一字未动**）；导入 `req` + `setupShellOverflow`；删除该段（留下 1 行"已搬至…"路标） ⇒ **811 → 738 行** |
| `tests/css-budget.spec.ts` | `main.ts` hard **900 → 780**（棘轮只收紧）+ note 说明本次外移 |
| `tests/panel-unify.spec.ts` | 该段的 4 条静态锚**改指新模块**（`shellOverflowSrc`），**断言一条不减、不弱化**：仍是「存在 `setupShellOverflow` / `ResizeObserver` / `MutationObserver`」+「判据体只用 `document.documentElement.clientWidth` 与 `desiredTabsWidth`，且**不得**出现 `bar.clientWidth`」✓ |

## 三、零行为变化的决定性证据（逐字比对）

```
搬移后模块体 vs 从 main.ts 删掉的那段：
  逐字相同 = true | before 长度 2992 | after 长度 2992 | 首个不同位置 null
```
（归一化**只允许两处**：① `$(` → `req<HTMLElement>(`（`$` 已变别名）② 新模块必需的 `export ` 前缀 7 字符 ✓）
脚本：`.verify/phase3b/verify-37.mjs`（比对失败即 EXIT 1）✓

## 四、验收

| 项 | 结果 |
|---|---|
| `pnpm run check`（tsc + eslint + 全部 vitest） | **EXIT 0** —— 36 文件、**与改前逐项一致**（同一批 519 passed / 1 skipped）✓ 即"改动前 e2e 基线"的逐项对照 ✓ |
| `cargo test`（跑前已清残留 `msedgewebview2`） | **EXIT 0** ✓ |
| 敏感检查 | **EXIT 0**（152 文件 / 17 模式）✓ |
| 行数 | `main.ts` **811 → 738**；`shell-overflow.ts` 79；`dom.ts` 18 ✓ |
| 新模块是否入仓 | `src/app/dom.ts` / `src/app/shell-overflow.ts`（untracked → 本次提交入库）✓ |

### 真机检查（搬移链路：标签增删触发判据）
探针 `.verify/phase3b/probe-37-overflow.mjs`（隔离 profile、优雅关闭、受信清单还原一致 ✓）：

| 状态 | 读数 | 判定 |
|---|---|---|
| 清空后 | `overflow=false`、`want=26`、`avail=1212` | **正确** ✓ |
| 打开若干文档后 | `overflow=false`、`want=238`、`avail=1212`、4 标签 | **正确**（远未装不下）✓ |
| 再清空 | `overflow=false`、`want=26` | **反向正确** ✓ |

⚠ **如实**：**`overflow=true` 那一支本轮没在真机上复现** ✗ —— 原因**是我的探针设计错**：
我传了 6 次参数但只有 **2 个不同路径**（应用按路径去重）⇒ 标签少且名字短 ⇒ 总宽远小于可用宽 ✗（**不是产品问题**）。
该分支由 (a) 逐字相同的搬移 ✓ (b) 该段注释里保留的**批次 2 真机实测**（162 采样点只翻 3 次、单调 ✓）
(c) 静态锚（判据体两条）三方覆盖 ✓

## 五、本批"工具/脚本自己错"家族（累计 14 例）——3-7 一次贡献 5 例

| # | 谁 | 错法 | 后果 / 处置 |
|---|---|---|---|
| 9 | 补丁脚本 v1 | 假设 `print-ready.ts` 有 `import ` 开头的行（**该文件没有任何静态 import**）✗ | **原子失败、零改动** ✓ |
| 10 | 补丁脚本 v2 | **补丁自己的注释里写了注释结束符** ⇒ 注释提前闭合 ⇒ SyntaxError ✗ | **零改动** ✓ |
| 11 | 我的 `git grep` | 用 `-- 'modu/src/**/*.ts'` ⇒ **不匹配 `src/` 直属文件**（1 条 vs 正确 5 条）✗ | 差点漏判"符号没人用" ⇒ 改用目录 pathspec / `-- '*.ts'` ✓ |
| 12 | 补丁脚本 v2 | `String.replace(str, str)` 里我写了 `` `$` `` ⇒ `` $` `` 被当成"**匹配前的全部内容**" ⇒ **把 `main.ts` 写坏（812 行）** ✗✗ | `git checkout` 还原 + 改**函数式替换**（关闭 `$` 语义）✓ **建议进全局笔记** |
| 13 | 补丁脚本 v3 | **先写 main.ts、再校验 spec 锚点** ⇒ spec 锚因 CRLF 不匹配抛错 ⇒ **部分应用** ✗ | 改**两阶段：先全验后全写** ✓（并把 CRLF 探明：两文件均为 CRLF ✓）|
| 14 | 我的探针 | 对象字面量键 `6标签:` **不能以数字开头** ⇒ SyntaxError ✗；另：CRLF 下 `lines[i] === '}'` 判不中 ✗ | 分别修 ✓ |

⇒ 与第 1–8 例同族：**「测出来的 ≠ 事实」**；本轮 5 例全部由"**原子失败 / 两阶段校验**"兜住或当场发现 ✓

## 六、3-7 剩余（未完成，如实）

1. **`tabs.ts` 未拆**（现 **621 行**，hard 700 ✓ 未超）：本轮只动了 `main.ts` ⇒ **未给 `tabs.ts` 收紧棘轮**（没拆就不收紧 ✓）
2. **`main.ts` 仍 738 行 > soft 400**：可继续外移的候选（按依赖干净程度排序，供下一轮）
   - 「全局键位」`setupGlobalKeys` + 「标签快捷键」`cycleTab`/`setupTabHotkeys`（L~316-384，依赖 `tabs`）
   - 「关闭守卫」`unsavedText` + `saveDirtyTabs`（61 行，依赖 `tabs` + overlay UI）
   - 「导出 PDF」`defaultPdfName` + `onExportClick`（L~254-310，依赖 `tabs` + 状态栏）
   - 「窗口标题栏」`syncMaxState` + `setupWindowControls`（依赖 `closeGuard` ⇒ 需先定义清晰边界）
   - `boot()`（132 行，最大块）⇒ **最后再拆**（它是装配点，拆它收益大但风险也最大）
3. 建议下一轮沿用本次模式：**挑一个依赖干净的自洽段 → 逐字外移 → 锚同步迁移 → 收紧 hard → 三项门禁 + 真机检查** ✓
