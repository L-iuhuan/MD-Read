# Phase 3 · 批次 3 · 3-2「说谎的注释」系统扫描报告（2026-09-23）

- 纪律：**未起应用** ✓（3-4 未修之前不跑探针）；每条**用代码/`git` 核**，不信旧报告结论。
- 四类：① 注释与代码事实矛盾 ② 写"待裁决/未实现"但已裁决/实现 ③ **写死的数字**与当前不符 ④ 断言被后续提交改掉。

## A. 已修项（4 条）

| # | 文件:行 | 原文（要点） | 矛盾点 | 依据 | 改法 |
|---|---|---|---|---|---|
| 1 | `docs/specs/…设计规格.md` L1 + `modu/AGENTS.md` L3 | 规格标题「v1.1」、AGENTS「（v1.1）」 | **标题落后 §10 变更记录两版**（记录已到 v1.3） | 读文件实见 §10 有 v1.2/v1.3；**发现方式=我核文件发现会撞号**（Lead 原判"下一个是 v1.2"） | 标题 → **v1.4**、AGENTS → **（v1.4）**、§10 追加 v1.4 行（并置于 v1.3 之后） |
| 2 | `docs/tasks/Phase2-前-决策登记…md` D-22 行 | `\| D-22 \| D-08 主题变更合规 \|` | **坏引用**：D-08 实为「文档脱敏时机」，与主题无关 | 同表 D-08 行原文；**发现方式=Lead 核交叉引用** | 登记表 → `**D-01** 主题变更合规（**原表误写 D-08**）`（**误写留痕**）；规格 §9 该行**加括注**、原始措辞保留 |
| 3 | `modu/src/app/outline-follow.ts` L5-8 | 「旧实现 observeHeadings 对 4,715 个标题逐个 observe，Blink 每帧重算相交矩形 —— `computeIntersections` 占墙钟 26.3%~33.6%，是两轮 trace 的第一名单点」 | **断言被后续提交改掉（第 ④ 类）**：X2 之后的三组决定性实验**推翻**该归因 —— ①去掉标题观察器 → 计数器**仍 1,790.6ms/6s**；②加回"观察 4,715 标题"→ 只 **+0.8ms/帧**；③关掉 `content-visibility:auto` → **4.7ms** ⇒ 真正主因是 `content-visibility:auto` | `AGENTS.md` 性能归因节（三条实验原文）、《性能实验报告》文首勘误 | 改成"**归因已勘误**"表述：保留原数字 + 写明三条实验 + **明确真正主因**；并注明"下文『实时几何 vs 预计算偏移表』的理由不依赖该归因，仍成立" |
| 4 | `modu/AGENTS.md` L3 后 | —（新增） | 版本漂移**不该靠人记得核** | 本次实际翻车 | 新增**静态锚** `modu/tests/doc-version-anchor.spec.ts`：断言「规格 L1 标题版本 == §10 最后一条变更记录 == AGENTS.md 引用的版本」；`docs/specs` 缺失时**跳过不假红**；失败信息中文且**三处编号都打印** |

## B. 扫过但**判定为准确**（不改，供复核）

| 文件:行 | 原文（要点） | 为什么不算说谎 |
|---|---|---|
| `typography/cjk.css:58` | 「`hanging-punctuation` 在 Chrome 未实现，故不写 —— 不留死代码」 | 能力判定**准确**（项目纪律允许留档能力说明） |
| `typography/cjk.css:405` | 「备选 B（**未实现**，留档）」 | 明确标注"未实现/留档"，是**有意的设计留档**，非过期断言 |
| `typography/print.css:7` | 「禁 `@page` margin-box —— 历史上 Chromium 未实现…」 | 与 `AGENTS.md` 同口径（历史未实现 + 本项目经评审不用）✓ 准确 |
| `app/tabs.ts:534` | 「正文留待末项统一挂载」 | 描述**代码后续步骤**（未来时），非状态断言 |
| `render/mermaid.ts:113` | `rootMargin: '200px 0px'` + 「提前 200px 预渲染」 | 与代码一致 ✓ |
| `render/markdown.ts:148` | 「7 列财务表实测溢出纸面 91px」 | 历史实测记录（属已修缺陷的成因说明），未见被后续改动推翻 |
| `render/print-ready.ts:144-163` | 658/987、734px、1870px 等 | **本次同批刚写入**的实测值 ✓ 与 `print.css`/报告一致 |

## C. ③ 类"写死的数字"专项核对（行数棘轮）

| 量 | 注释/文档写的 | **实测** | 判定 |
|---|---|---|---|
| `src/main.ts` | `tests/css-budget.spec.ts`：soft 400 / **hard 900**（无注释写具体行数） | **810 行** | ✓ 未超 hard（810 ≤ 900）；⚠ **Lead 口述的"804 行"已过期**（本批改动 +6 行）——这是**人给的数字**过期，不是注释过期 |
| `src/app/tabs.ts` | 同上：soft 400 / **hard 700**，note 写"拆分排在 Phase 2.5" | **604 行** | ✓ 未超 hard；note 里"Phase 2.5"仍是**未发生的将来**（3-7 拆文件在批次 3）✓ 不算谎 |

⇒ ③ 类**没有发现新的过期数字**（除上面 Lead 口述那条）。

## D. 本轮**未完成**的部分（如实）

1. **未做全仓逐文件通读**：本轮以**高产出模式**扫描（`待裁决|待用户|待批次|待定|未实现|暂不|将来再|留待` + `\d+ 行|\d+ 个块/节点|约?\d+px|\d+ms/帧|\d+FPS` 两个正则全 `modu/src` 覆扫 + 棘轮实测），
   共 4 个 ② 类命中、25 个 ③ 类命中，**逐条看过并分类**（B/C 表）。**未覆盖**：`docs/**` 内的历史报告之间的相互矛盾、
   以及 `.verify/**` 探针脚本的注释（后者是测试脚手架、gitignore）。
2. **`git log -S`/`blame` 只用在第 3 条**（`outline-follow.ts` 的归因被 X2 推翻）。其余命中项靠**读代码/读 CSS 现值**判定，
   未逐条 blame —— 若你要更强的证据链，我可以对 B 表逐条补 blame。
3. 已知的另一条候选（`tokens.css:345` 的 `--bg-th`「故意不接线」）**留给 3-5** 专项处理，未在此重复。

## E. 对 B 项锚（Lead 建议）的判断：**值得做，已做**
- 理由：这条漂移**刚刚真实发生**（标题 vs 变更记录差两版），且**成本极低**（读两个文件、一次正则比较）；
  不会挡正常文档编辑（只要求"标题与最新变更记录同号"，改版本时同步标题本就是该做的事）。
- 降级已按要求实现（`docs/specs` 缺失 ⇒ 跳过）✓ 失败信息中文且**打印三处的实际编号** ✓。
- ⚠ 一处**代价**如实说明：它把"§10 末条 = 当前版本"确立为契约 ⇒ **将来若在 §10 中间插入历史补录行**（就像我这轮先插在 v1.3 前），锚会**立刻失败**（本轮它就抓到了我这个错 ✓）——
  这是**特性不是缺陷**，但写文档的人要知道"补录行要按时间顺序追加在末尾"。
