# 批次 3 · B3 报告：GitHub Alert（我们自己的扩展）着色 + 非颜色通道

日期：2026-09-23 ｜ 执行：modu-shell

## 结论
5 类 Alert（NOTE/TIP/IMPORTANT/WARNING/CAUTION）**只在引用块首行恰好是 `[!TYPE]` 时识别**（大写白名单，普通引用块**逐字不变** ✓）；
配色**全部派生自既有 token**（零新字面色值 ✓）；**非颜色通道**：中文+英文文本标签 + 每类**几何字形**（◆ ✓ ※ ▲ ✕，文本呈现、非彩色 emoji）；
打印**不铺底**、色条保留 ⇒ 纸上可读 ✓

## 一、实现（4 改 + 1 新）
| 文件 | 改动 |
|---|---|
| `src/render/markdown.ts` | 白名单 token 插件（首行恰好 `[!TYPE]`；剥标记；标记独占整段才删空段落）|
| `src/typography/tokens.css` | 5 组 alert token，**全部 `color-mix` 派生**（底 = 族色小比例混 `--bg-quote`、墨 = `--text`）⇒ 亮/暗自适应、零字面色值 |
| `src/typography/cjk.css` | `.mdc blockquote.alert-*`（基类 + 5 类），**标签与字形都在 `::before` 的 content 里**（非颜色通道）|
| `src/typography/print.css` | 打印：**不铺底**（最保守）+ 保留左色条 |
| `tests/alert.spec.ts`（新，6 条）| 识别矩阵 + **普通引用块 HTML 逐字不变** + 金额串/字面量 |

**锚当场抓到我的一个数据丢失级 bug**：`> [!NOTE]\n> 正文` 是**同一段**的 `text+softbreak+text` ⇒ 我第一版"空内容就删整段"把 **alert 正文一起删了** ✗ ⇒ 已修（只删空 text 与紧随 softbreak）✓ —— 测试价值现场兑现。

## 二、验收（两套口径**并列**）
### ① 判据 = **像素**（Lead 口径；产物落盘 `b3-pixels.json` / `.csv`）
- **50/50 块**（5 调色板 × 亮/暗 × 5 类）**正文与标签行对比度均 ≥4.5:1** ✓
- 区间：**正文 14.76–16.56**、**标签行 14.76–16.56**（远超 4.5）✓
- 暗色打印与亮色打印**各出一张 PDF（字形之后重出）** ⇒ `pdftoppm` → **两张都看过图** ✓
  （暗 254,184 字节 / 亮 254,067 字节；5 类字形+双语标签+左色条齐、底色透明、普通引用块无标签 ✓）
- **字形进 PDF 文本层**（决定性）：`pdftotext -layout` 里 **◆ ✓ ※ ▲ ✕ 五个字形全部出现**，标签行逐条为
  `◆ 注意 · NOTE` / `✓ 提示 · TIP` / `※ 重要 · IMPORTANT` / `▲ 警告 · WARNING` / `✕ 当心 · CAUTION`
  ⇒ **非颜色通道真的上纸，而不只是"我看到了"** ✓

### ② 已弃 = `getComputedStyle` + canvas（**量具错**）
- 该路曾报"亮色 5 组全部 **1.33:1**" ✗ —— **假警报**：Chromium 把 `color-mix()` 序列化成 `color(srgb …)`，
  而 **canvas 的 `fillStyle` 赋这种字符串会被静默忽略、回读成黑** ✗ ⇒ 得到"黑 vs 深墨 = 1.33"
- ⇒ 与像素路（14.76–16.56）**不一致**，**以像素为准** ✓；两个数都留在案 ✓

## 三、NOTE vs IMPORTANT 的非颜色差异
- **文本/字形不同**：`◆ 注意 · NOTE` vs `※ 重要 · IMPORTANT` ✓（`pdftotext` 文本层证据 ✓ + 两张看图 ✓ —— 两者肉眼可辨 ✓）
- ⚠ **`6px double` 边框方案实测无效**（computed 全 4px/solid、像素逐列与 NOTE 相同）⇒ **该 CSS 已删除** ✗
  （留着就是"会撒谎的代码"）；**原因未定位**（未编理论）
- ⚠ **"字形墨色 = token 色"的自动量失败（如实）**：我做的"只裁字形小块"量具，其**左色条检测先撞上页边白/页底** ⇒
  裁窗落偏、比对的 token 色其实是页边色 ⇒ 距离数（388）与"高饱和色≥2"（25/50）**均无效** ✗
  ⇒ **不以这些数字作结论**；非颜色证据以**文本层 + 两张看图**为准 ✓；量具待修（色条检测需跳过页边）✗

## 四、门禁（三项 EXIT）
`pnpm run check` **EXIT 0**（38 文件、530 passed | 1 skipped，含 golden-HTML 对拍）｜`cargo test` **EXIT 0**｜敏感检查 **EXIT 0**

## 五、判据族（累计 19 例）
本轮新增：**#18** 把"整块正文墨色"当成"字形墨色"（写检查时留了占位表达式 ✗）｜**#19** 色条检测撞页边 ⇒ 裁窗落偏、
造出"多色"假阳性 ✗。共同根因仍是 **「测出来的 ≠ 事实」**；处置：**产物落盘 + 只用可靠口径 + 不明数字不作结论** ✓
