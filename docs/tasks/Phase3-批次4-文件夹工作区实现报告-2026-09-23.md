# 批次 4 · 文件夹工作区（D-11）实现报告

日期：2026-09-23 ｜ 执行：modu-shell ｜ 状态：**提交① ②已落地并推送；③ 探针已跑完留证；③ 剩余前端树面板与文案待做**

> 门禁数值不写进文档（每轮都变）：跑 `pnpm run check` / `check:full` / `cargo test` 看当前值。

## 一、设计（已过 Lead 审，落库）
`docs/tasks/Phase3-批次4-文件夹工作区设计报告-2026-09-23.md`（含 §6 **9 条验收** + 3+3 条补充）✓

**前提纠正（实测）**：规格 `:48`/`:141` 与 `docs/开发状态.md:58` 把「文件夹工作区与跨文件搜索」列为**二期 ✂️**，
**D-11 只是待立决策** ⇒ 本设计＝立项提案；Lead 已批"规格改一行"须**与实现同批落地** ✓

## 二、已交付（均已推送）

| 提交 | 内容 | 证据 |
|---|---|---|
| **`b0fc67b`** | **提交①**：`TrustedPaths::allowed_for_list_dir`（列目录判定，与读路径**同源**按**路径分量**；**fail-closed**：规范化失败时只有父目录受信才给可行动文案，其余一律 `Untrusted` ⇒ **不泄露存在性**）+ 单测 **+2**（`..` 逃逸的**读与列目录**两条都拒 + 正对照；列目录四条：本身/子目录/**兄弟（字符串前缀相似）**/之上）| `cargo test` 48 passed（改前 46 ⇒ +2）|
| **`db6f129`** | **提交②**：`list_dir(path, limit)` + `list_dir_at()`（**单遍** `read_dir`、**收满 `limit` 即停**、`total` 恒 `None`、顺序**目录在前→名称码位序**、**零内容读取**（只 `read_dir`/`file_type`/`file_name`）、**符号链接不跟随**、拒绝吃 `DenyReason`/`deny_message`）+ `pick_workspace_directory()`（**授权**入口，**不接受路径参数** ✓，**取消 ⇒ `Ok(None)`** ✓）+ `lib.rs` 注册两条 | `cargo test` 50 passed（改前 48 ⇒ +2）；两条新测试实跑通过 |
| **`6086496`** | 流程节补通用规则：**「先改环境再启动」的探针必须自己收尾**（见 §五）| — |

## 三、③ 探针：**渲染层路径的实测证据**（四次调用，全部走 `window.__TAURI__.core.invoke`）

前置：`window.__TAURI__ 存在 = true` ✓（`tauri.conf.json` 已含 `withGlobalTauri` ✓）· `__moduDev 就绪 = true` ✓（**无固定 sleep** ✓）
受信目录靠**启动前种入**清单 `dirs`（canonical 小写形态 `\\?\d:\files\…` ✓），并已在跑后**手工写回启动前原文** ✓

### ① 正例（受信目录）⇒ DirListing
```json
{"ok":true,"value":{"path":"<项目根>\\...",
 "entries":[{"name":"子目录","is_dir":true,"is_markdown":false},
            {"name":"a.md","is_dir":false,"is_markdown":true},
            {"name":"b.md","is_dir":false,"is_markdown":true},
            {"name":"z.txt","is_dir":false,"is_markdown":false}],
 "truncated":false,"total":null}}
```
⇒ `entries` 数组 ✓ · **顺序 = 目录在前 → 再码位序** ✓（`子目录`(U+5B50) 排在 `a.md` 之前 ⇒ **单凭码位序不可能** ⇒ 确证"目录优先" ✓）· `truncated:false` ✓ · **`total:null`** ✓（单遍、不为 total 多遍历 ✓）

### ②③④ 三种越界（全部拒，且 ②③ 同模板）
```
② 存在：  无法浏览文件：…\phase4（该文件不在本次已打开的清单中，请用「打开文件」重新选择）
③ 不存在：无法浏览文件：…\phase4\不存在（同上文案）
④ ..逃逸：无法浏览文件：…\ws\..（同上文案）
```
⇒ **剥掉路径后 ②③ 模板完全相同** ✓ ⇒ **存在性不泄露在渲染层也成立** ✓
（⚠ 探针脚本首版断言写成"含路径整串比较"⇒ 假红 ✗；已按"剥路径比模板"重判 ✓）

## 四、capability 结论（以**真实调用**为准，不靠构建）
**`capabilities/default.json` 未改动** ✓（仍 455 字节、仅 `core:*` + `opener:*`）—— 而**四次调用都按预期返回** ✓
⇒ **无需新增 capability 条目** ✓✓（"构建通过"不能证明可调用 ✗，本结论由渲染层真实调用得出 ✓）

## 五、环境收尾与可核证据
- `modu = 0` · **端口 9222 = 0** ✓ · 工作树 0 项 ✓
- 包装脚本日志：`[trust] 启动：argv 注册 1/1，清单现有 1 文件 / 1 目录` ✓
  ⇒ **种入的目录条目启动后仍在** ⇒ **排除"store 写回丢 dirs"的产品 bug** ✓
- `[还原(child-exit)] 受信清单 存在=true sha=4123f7c6fc1518f6 与启动前一致=true` ✓
- ⚠ **探针自己收尾（必需，不是保险）**：包装脚本的快照在**启动时**拍，而种入在**启动前** ⇒ 它还原回来的是**带测试残留的那份** ✗
  ⇒ 已**手工写回启动前原文**并核对：`files` 2 条 ✓ · `dirs: []` ✓ · **无 phase4 残留** ✓
  （该规则已写进 `modu/AGENTS.md` 流程节 ✓，提交 `6086496`）

## 六、验收进度（设计 §6 的 9 条）
| # | 验收 | 状态 |
|---|---|---|
| ② | `..` 逃出必拒 | ✓ 提交① 单测（读 + 列目录）+ ③ 渲染层 ④ 号调用 ✓ |
| ④ | 分量边界（`C:\a\b` vs `C:\a\bc`）| ✓ **既有测试**（`trusted_dir_matches_descendants_by_component_not_string_prefix`）+ 提交① 的兄弟目录断言 ✓ |
| ③ | 渲染层无注册命令 / **命令边界** | ✓ ③ 探针（越界存在/不存在同模板 ✓）· **渲染层不存在"注册信任"命令** ✓（`trust_directory` 刻意不是 `#[tauri::command]` ✓）|
| ⑨ | 上限值有实测依据 | ✗ **待做**（提交④：量 200/500/1000/2000 首屏耗时后定值）|
| ① | 目录外路径仍拒 | ✓ 提交① / ③（② 号调用）|
| ⑤ | 撤销路径可用 | ✗ 待做（侧栏"移除工作区"）|
| ⑥ | 不可读目录显示原因 | ✗ 待做（前端）|
| ⑦ | 切换回大纲状态不变 | ✗ 待做（前端）|
| ⑧ | UI 写明"其中的文件可被打开并编辑" | ✗ 待做（前端文案）|

## 七、③ 剩余工作（下一步，按设计 §6，不加新要求）
- **前端树面板本体**：与侧栏 ③ 大纲**共用侧栏**、标题栏「大纲 / 文件夹」切换；**逐级懒加载**；
  点文件按既有去重开标签；不做多选
- **UI 文案**：空状态（"还没有工作区 —— 选一个文件夹开始" + 按钮）· **"其中的文件可被打开并编辑"** ✓
  · 拒绝文案的 **「文件 → 目录/位置」**修正（本次动作为列目录，现文案说"该**文件**不在…" ✗）
- **验收 ⑥⑦**（不可读显示原因不崩 / 切回大纲状态不变）+ **提交④**（上限先量后定）+ **提交⑤**（规格那一行 + 撤销路径）
- 老规矩：`cargo test` 前杀 `msedgewebview2` ✓ · 提交走 `commit-with-gates --full` ✓ · `tabs.ts` 不碰 ✓

## 八、本批教训（已入 `modu/AGENTS.md`）
1. **「先改环境再启动」的探针必须自己收尾** ✗（包装脚本只覆盖"被测程序自己的写"）✓
2. 判据测试 **证明不了命令用了它** ⇒ 必须有**命令边界**负对照 ✓
3. "构建通过" **不能**证明"可调用" ⇒ capability 结论以**渲染层真实调用**为准 ✓
4. 顺序断言要**从数据读出**（`子目录`(U+5B50) 先于 `a.md` ⇒ 只能来自"目录优先"），不是"看着像" ✓
