# Phase 3 · 批次 3 · 3-3 登记报告：mdput 深挖 6 项补录 D-12（2026-09-23）

- **只登记、不实现**；6 项**均未实现、未排期**，均标 `⏸ 待用户裁决是否做`。
- 登记位置：`docs/tasks/Phase2-前-决策登记-2026-09-23.md` 新增节「**D-12 补充登记（2026-09-23 · 批次 3 · 3-3）**」，插在 M10 行之后、"明确不做（15 项）"之前。
- 编号写法：`A2(mdput深挖 §6.3)` 等 —— **刻意带来源章节**，与本表 `M1–M10` 同源不同清单，**避免与别处编号撞车制造新的坏引用**（今天刚修过一条 D-08/D-01 坏引用）。
- `docs/tasks/` 在 `.gitignore:31` ⇒ **本件无提交**（仅本地留档）。

## 我的逐条判断（Lead 要的"值得做/建议不做/规模"）

| 项 | 一句话 | 规模 | 我的判断 | 理由（关键是**是否依赖旧前提**） |
|---|---|---|---|---|
| **A6** | 异步代际令牌：await 后校验票号，防过期渲染回填 | **S（≈5 行）** | **✅ 建议做** | **正确性缺陷**（缓存命中路径现无票号校验），且与《代码审查报告》§4.2 / B4 **互相独立印证** ⇒ **不依赖任何"编辑体验"旧前提**，投入产出比最高 |
| **A5** | Mermaid 语法预校验 + 失败保留源码标红 | S（≈30 行 + 3 CSS） | **🟡 可做（价值收窄）** | ⚠ MD-Read **已有**失败通路：渲染侧有 `data-mmd-error` 失败占位、导出侧 `print-ready` 会按源码兜底 ⇒ 本项净增的只是"**提前** parse 校验 + 失败标红"，不是从无到有 |
| **A7** | 打开对话框收 `.txt` | S（改 accept 列表） | **🟡 待用户定（产品范围）** | 0 KB、编码检测与渲染管线对 .txt 完全适用，财务/日志场景常见；**唯一风险是产品边界**——⚠ 只动"打开对话框"accept，**不动 `fileAssociations`**（否则抢占系统 .txt 关联） |
| **B3** | GitHub Alert（`> [!NOTE]` 等 5 类）→ 着色引用块 | S（≈25 行 + 5 CSS） | **🟡 可做但优先级低** | ⚠ **Alert 是 GitHub 扩展、不是 GFM**（规格 F2 只承诺 GFM + 脚注 + 锚点 + KaTeX + hljs + Mermaid）；今天 `> [!NOTE]` **只是没着色、不是坏**⇒ 锦上添花 |
| **A2** | 编辑↔预览同色联动高亮 | S（≈35 行 + 2 CSS） | **❌ 建议先不做（P0 来自旧前提）** | ① 原报告的 P0 是**三件套 A1→A2→A3** 里的一件，而 **A2 明确以 `A1 data-line-end` 先落地为前提**；A1 **不在**这 6 项、也未登记 ⇒ **单独做 A2 前提不成立**；② 它服务的是"**编辑↔预览感知通道**"，而同一份报告 **§5.3 已裁决不上双栏**、产品定位**阅读为主** ⇒ 收益面只在编辑态 |
| **B5** | 预览重渲染按小数行保形（块内比例） | **M（≈50 行）** | **❌ 建议不做** | D1 已明确"块级定位验收降级（inline 无 map）"，块顶对齐已能工作；收益仅"重绘后滚动更精确"却**触碰 D1 语义** ⇒ 六项里性价比最低 |

## 建议的取舍顺序（若用户要做）
**A6（正确性）→ A7（产品范围 0 KB）→ A5（价值收窄）→ B3（锦上添花）**；
**A2 / B5 建议不做**（A2 前提缺失 + 定位偏编辑态；B5 触碰 D1 且收益最低）。

## 诚实标注
- A2 的"P0"我**没有照抄**：它来自《深挖》§6.3 的优先级表，而该表把 **A1/A3 也列为 P0**（这两项**不在**本次登记范围）；
  A2 的实现前提（A1）缺失这一点写在原报告 §4.1 表格里，**本次已显式登记进 D-12 状态列**。
- 6 项的影响面/规模均照抄原报告结论（S/M 与行数估计），**我未独立估算**（未读它们要改的具体代码，属"只登记"边界内）。
