# Phase 3 · 批次 3 · 3-1 文档真相对齐报告（2026-09-23）

- 纪律：**只补事实、不重新设计**；每条列出「原文 → 改后 → 依据」。**未起应用**（遵守 3-4 之前的禁令）。
- 改动的文件：`docs/specs/2026-09-21-墨读设计规格.md`（**tracked**，本批唯一入库改动）；
  `docs/tasks/Phase2-前-决策登记-2026-09-23.md`（gitignore，仅本地留档）。
- 依据来源**全部是代码/既有报告**（不看旧报告的结论）：
  `modu/src/typography/tokens.css:1-41,223-320`、`modu/src/ui/theme.ts:11-73`、`modu/src/main.ts:124,585`、
  `modu/src/ui/settings.ts:15,171-225`、`docs/tasks/Phase2-前-决策登记-2026-09-23.md`（§D-01/§D-22/§D-12 表 M10）、
  `docs/tasks/性能实验报告-2026-09-23.md:364,378`。

## 1. 5 套主题体系写进规格（线索：`docs/specs` grep `靛蓝|调色板|5 套|data-palette` 原为 **0 命中**）

**改动 1 —— §2.1 F9（规格 line 36）**
- 原文：`| F9 | 主题：亮暗双套 + 跟随系统；字号/行宽可调并持久化 | P1 | |`
- 改后：保留原句，**追加**「（2026-09-23 补录事实）实际已实现为 **5 套调色板 × 亮/暗 = 10 组**：
  `data-palette` ∈ `dianlan`（靛蓝，默认）/ `xuanzhi`（宣纸）/ `shimo`（石墨）/ `zidai`（紫黛）/ `qingmo`（青墨），
  与 `data-theme`（`light`/`dark`）**正交**；持久化键 `modu-palette`（无记录/坏值回退 `dianlan`）」
- 依据：`tokens.css:225/235/245/255/265`（5 组亮态选择器）+ `tokens.css:276-320`（5 组暗态）+
  `ui/theme.ts:11-14,26,69-73`（`PALETTE_KEY = "modu-palette"`、`data-palette` 写入、回退 `dianlan`）+
  `main.ts:585`（启动即 `applyPalettePref(readPalettePref())`）

**改动 2 —— §3.3 主题与排版 demo 协议（规格 line 81-82，新增两条）**
- 原文（该节只有一句「亮暗双套成对设计、同步验收」）：
  `- 亮暗双套成对设计、同步验收；正文对比度 ≥4.5:1；语义色红错绿过黄警蓝信`
- 改后：**保留原句**，其后新增：
  - 5 套 × 亮/暗 = 10 组的事实 + **色值纪律已被 D-01 修订**（新 5 套**不属 Radix 体系**；
    **红绿黄状态色未换体系、仍取 Radix 阶数**）
  - **token 三层结构**（① 共享层 `:root` ② 调色板层 `--pal-*` 只声明原色 ③ 语义映射层 `--bg-*`/`--text-*`/`--accent-*`，
    UI 与正文只认第 ③ 层；改色只改第 ② 层；派生量一律 `color-mix` 不新造字面色值）+ **DOM 契约四条** + 顺序纪律
- 依据：`tokens.css:4-8`（三层）、`:15-20`（色值纪律与覆盖面）、`:18-20`（color-mix 派生）、`:23-32`（DOM 契约 + 顺序纪律）

## 2. F9 的"亮暗双套"（线索：F9 仍写"亮暗双套"）
- 见改动 1：原句保留、事实追加（**不是**把原句改掉 —— 保守对齐，保留设计意图原文）

## 3. D-22 主题变更追认**补录入 §9 台账**（线索：宪法要求「修订须用户批准并记入规格 §9」，此前只活在 `决策登记` 表）
- 原文：§9 台账最后一行 = `| 2026-09-23 | PDF 导出载体由 ICoreWebView2::PrintToPdf 变更为 CDP Page.printToPDF … |`
  （**无主题变更行**）
- 改后：其后新增一行 ——
  `| 2026-09-23 | **D-22：D-08 主题变更合规「追认」** —— 主题体系由「亮暗双套」变更为 5 套调色板 × 亮/暗 = 10 组，
  `data-palette` 为与 `data-theme` 正交的新维度；色值纪律由「只从 Radix 阶数取」改为「新 5 套不属 Radix 体系
  （状态色仍取 Radix）」。**补录入本台账**（补录原因：宪法要求…此前只活在 `决策登记` 表里） | 用户追认 | … |`
- 依据：`决策登记` §D-22（`| D-22 | D-08 主题变更合规 | **追认** | 台账记录 | ✅ 定案 |`）+ §D-01 +
  `Phase2-D01-主题token化报告` + `tokens.css`（10 组）+ `ui/theme.ts`

## 4. `main.ts:124` 与 D-12「M10」的状态矛盾（线索：代码已判"不做"，登记仍标"待做"）
- 事实（代码为准）：`modu/src/main.ts:124` 注释原文「M10「滚动节流开关」**已判不做**」；
  性能侧也不支持：`性能实验报告` §364/§378 —— 直接 scroll 监听 self-time 仅 **1–7ms/帧**，
  节流最多省 **<5% 墙钟**却引入"状态与视口不同步"风险，结论「**降级为可选、不建议做**」。
- 原文（`决策登记` D-12 表）：`| **M10** | 预览刷新节流开关 | … | Phase 2 | 待做 |`
- 改后：状态列 → **`⚰️已作废`** + 标明依据（`main.ts:124` 注释原文 + 性能报告 §364/§378 的实测数字）
- 依据：`modu/src/main.ts:124`、`docs/tasks/性能实验报告-2026-09-23.md:364,378`
- ⚠ 该文件在 `docs/tasks/`（gitignore）⇒ **只在本地留档**；规格侧不含 M10（它是 Phase 2 的 mdput 借鉴项），
  故以"登记表对齐代码"为准，本报告即留痕。

## 5. 本轮**未改**（拿不准 / 属设计意图，列出来问）
1. **§10 变更记录没加 v1.4**：3-1 的三处都是"补录事实/补台账"，不是设计变更；要不要在 `## 10. 变更记录`
   追加一条（例如 `v1.4（2026-09-23）：补录 D-22 主题体系（5×2）与三层 token 结构；F9 同步事实`）？
   **等你定**（我倾向加，但要你确认口径）。
2. **D-22 的"D-08 主题变更合规"到底指哪条变更**：登记表只写「D-08 主题变更合规 / 追认」，
   我在 §9 行里按 **D-01 主题 token 化（5×2）** 落到具体内容 —— 若 D-08 指的是**另一件**主题变更
   （例如"跟随系统"语义调整），请纠正，我再改。
3. **F9 原文的"跟随系统"**：`tokens.css` 的 DOM 契约只有 `data-theme ∈ {light,dark}`，
   没看到 `auto/跟随系统` 的解析落点（可能在上层 `theme.ts` 的 light/dark 解析里，我未展开核）。
   故 F9 里"跟随系统"我**一个字没动**（保守）；若要我补录其实现落点，给一句话我就去核 `ui/theme.ts`。
